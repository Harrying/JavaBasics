今日考察重点：对象流 内存流 Properties

作业1:
	从键盘录入学生信息，遇到quit结束录入。
	录入格式为 姓名:学号:年龄 例如: zs:5:19
	将录入学生封装成对象，最终全部写入  内存流中；
	
	从内存流中读取所有学生进行遍历。
	

作业2：
user.properties内容如下
id=2
name=jack
age=20

利用Properties获取配置信息，然后由提取到的值
实例化一个Student对象，最后将该对象写入到user.txt中。

作业3：
	在完成上述练习的前提下，
	
	利用对象流读取user.txt中的Student对象，
	然后从键盘录入一个新的name值，修改Student对象的名字
	最后将学生对象基本信息写回配置文件user.properties中。



