
作业1：
	自己实现课堂案例

作业2：
	定义一个内存输出流，通过子线程1 往里面 写入一个整形数、浮点数 
		借助DataOutputStream完成
	再实例化子线程2，从内存中读取出来 这两个数。
	
	注意，让线程1先运行，运行结束后，再运行线程2。	
	提示 sleep()
	
作业3：
	定义th1，逐行读取user.txt文件内容，每一行转换一个Student对象，
	然后存入List集合中；
	最后将对象写入到user.txt中。
文件内容如下:
	zs:002:23
	ls:010:12
	
	再定义th2,从user.txt中读取list对象，然后遍历。
	
	【注意，让th1先运行，运行结束后再运行th2。】
	

扩展作业:
	实例化两个线程，线程处理函数中都循环10次，
	一个输出A，另一个输出B，
	使用同步方法控制 交替打印A、B
	
	